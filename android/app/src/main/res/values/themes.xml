<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      이 앱에 어두운 화면은 없습니다. 폰이 다크 모드여도 밝게 둡니다 —
      웹에서 color-scheme: only light 로 막아둔 것과 같은 규칙입니다.
      forceDarkAllowed 를 끄지 않으면 시스템이 흰 배경을 임의로 뒤집습니다.
    -->
    <style name="Theme.MemoryMap" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowBackground">@color/paper</item>
    </style>
</resources>
