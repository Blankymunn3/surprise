# 사진 클라우드 저장 (Firebase Storage)

추억 지도(`map/`)에서 넣는 사진은 원래 **그 기기 브라우저(localStorage)** 에만 저장돼서
내 폰에서 넣은 사진이 아내 폰에서는 안 보였습니다.
지금은 Firebase Storage에 올라가서 **누가 어디서 넣어도 둘 다** 보입니다.

## 지금 상태

| 항목 | 값 |
|---|---|
| 프로젝트 | `our-surprise` |
| 버킷 | `our-surprise.firebasestorage.app` |
| 저장 경로 | `regions/<지역코드>.jpg` (예: `regions/11140.jpg`, `regions/C-JPN.jpg`) |
| 설정 파일 | [`assets/firebase.js`](assets/firebase.js) 맨 위 `CONFIG` |

## 남은 할 일 — 보안 규칙 게시

Storage를 켤 때 고른 **테스트 모드는 30일 뒤 만료**됩니다. 만료되면 사진이 안 올라갑니다.
콘솔 **Storage → Rules** 탭에 [`storage.rules`](storage.rules) 내용을 붙여 넣고 **게시**를 누르세요.

---

## 동작 방식

Firebase SDK를 받지 않고 **Storage REST API를 직접** 호출합니다.
폰에서 100KB짜리 SDK를 매번 받지 않아도 되고, 코드도 짧습니다.

| 상황 | 저장 위치 |
|---|---|
| 정상 | Firebase Storage → 양쪽 기기 모두 |
| 업로드 실패·신호 없음 | 이 기기 localStorage로 폴백 + "인터넷이 안 돼서…" 안내 |
| `CONFIG.storageBucket` 을 비우면 | 예전처럼 이 기기에만 저장 (기능 끄기) |

- 사진은 넣을 때 **가로·세로 최대 760px, JPEG 품질 0.72** 로 줄여서 올립니다 (장당 100KB 안팎).
- 업로드가 성공하면 그 지역의 localStorage 사본은 지웁니다 → 기기 저장 공간 부족 문제도 같이 해결됩니다.
- 목록은 localStorage에 캐시돼서, 다음 방문 때 사진이 먼저 뜨고 뒤이어 최신 목록으로 갱신됩니다.
- 다운로드 응답이 `Cache-Control: private, max-age=0` 이라 **사진을 바꾸면 다른 폰에서도 바로 갱신**됩니다.
- 목록 조회 요청에는 `delimiter=/` 가 반드시 붙습니다. 없으면 버킷 전체 목록으로 취급돼 규칙에서 막힙니다.
- 목록 조회 15초, 업로드 25초 제한이 걸려 있습니다. 넘으면 기기 저장으로 넘어가 사진을 잃지 않습니다.
- 지역 코드는 `[A-Za-z0-9_-]` 외 문자를 `_` 로 바꿔서 경로를 벗어날 수 없게 합니다.
- `photos/region-<코드>.jpg` 로 직접 올려둔 사진도 그대로 동작합니다 (클라우드 사진이 있으면 그쪽이 우선).

## 요금

무료 한도(5GB 저장, 하루 1GB 다운로드) 안에서 충분합니다.
사진 100장 = 약 10MB 라서 한도의 0.2% 수준입니다.
그래도 걱정되면 Google Cloud 콘솔에서 **예산 알림**을 1달러쯤으로 걸어 두세요.

## 더 안전하게 (선택)

지금 규칙은 **주소를 아는 사람은 누구나** `regions/` 에 사진을 올릴 수 있습니다.
사이트가 검색에 안 잡히고(noindex) 4자리 잠금이 있긴 하지만, 그 잠금은 브라우저 안에서만 도는 대문 잠금이라
서버 규칙에서는 확인할 수 없습니다. 더 잠그고 싶다면:

- **익명 로그인 + App Check**: Authentication에서 익명 로그인을 켜고 규칙을 `allow write: if request.auth != null` 로 바꾼 뒤,
  `assets/firebase.js` 에서 토큰을 받아 `Authorization` 헤더에 실어 보내면 됩니다.
  reCAPTCHA App Check까지 붙이면 남의 사이트에서 우리 버킷을 못 씁니다.
- **읽기까지 잠그기**: `allow read: if request.auth != null` 로 바꾸면 사진 주소를 알아도 로그인 없이는 못 봅니다.

필요해지면 말씀해 주세요. 그때 붙여 드리겠습니다.

## 문제가 생기면

| 증상 | 확인할 것 |
|---|---|
| "인터넷이 안 돼서 이 기기에만 저장했어" | 규칙이 만료됐거나(테스트 모드 30일) 게시가 안 된 상태 |
| 내 폰에서만 보이고 상대 폰에서 안 보임 | 목록 조회가 403인지 확인. `match /regions` 규칙과, 요청에 `delimiter=/` 가 붙는지 둘 다 필요합니다 |
| 사진이 아예 안 올라감 | 콘솔 Storage → 파일 탭에 `regions/` 폴더가 생기는지 확인 |
